import React, { PropTypes } from 'react';

const List = ({}) => {
  return (
    <ul>
       <li>List Item</li>
    </ul>
  );
};

List.defaultProps = {
};

List.propTypes = {
};

export default List;
